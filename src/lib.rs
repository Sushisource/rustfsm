#[allow(dead_code, unused_variables)]
pub(crate) mod card_reader;
// TODO: Convert to integ test if there ends up being any actual shared code / macro
pub mod card_reader_no_state_enum;
#[allow(dead_code, unused_variables)]
mod state_machine;
